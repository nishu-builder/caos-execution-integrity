const assert=require('node:assert/strict');
const fs=require('node:fs');
const path=require('node:path');
const {chromium}=require('playwright');
const data=JSON.parse(fs.readFileSync(path.join(__dirname,'../docs/trajectories/data/repair.json'),'utf8'));
const root=data.conversations.find(c=>c.id===data.root);
const text=(e,p)=>{const f=data.snapshots[e.tree].files[p];return f?data.blobs[f.oid].text:null};
const filename='code/dirty/test-discount.sh';
const weakened=root.events.find(e=>e.changes.some(c=>c.path===filename)&&!text(e,filename).includes('100 150'));
const bash=root.events.find(e=>e.records.some(r=>r.type==='tool'&&r.name==='bash'&&r.status==='complete'&&r.task));
const base=process.env.BROWSER_URL||'http://127.0.0.1:18184/trajectories/';
function url(event,tab='activity',file='') {return base+'?'+new URLSearchParams({example:'repair',conversation:root.id,event:event.oid,tab,file});}
(async()=>{
 const browser=await chromium.launch({headless:true});
 const page=await browser.newPage({viewport:{width:1500,height:1000}});
 const errors=[];page.on('pageerror',e=>errors.push(e.message));
 await page.goto(url(weakened,'files',filename));
 await page.waitForSelector('#browser:not([hidden])');
 assert.match(await page.locator('#facts').innerText(),/3 conversations/);
 assert.ok(!(await page.locator('.file-content pre').innerText()).includes('100 150'));
 await page.selectOption('#side','before');
 assert.match(await page.locator('.file-content pre').innerText(),/100 150/);
 await page.selectOption('#side','after');
 await page.reload();await page.waitForSelector('.file-content pre');
 assert.ok(!(await page.locator('.file-content pre').innerText()).includes('100 150'));
 await page.click('[data-tab="activity"]');
 assert.match(await page.locator('.diff').innerText(),/-.*100 150/);
 await page.screenshot({path:process.env.SCREENSHOT_DIR?path.join(process.env.SCREENSHOT_DIR,'browser-diff.png'):'/tmp/trajectory-browser-diff.png',fullPage:true});
 await page.getByRole('button',{name:'Bug fixed',exact:true}).click();
 assert.match(await page.locator('.diff').innerText(),/discount.sh/);
 assert.ok(new URL(page.url()).searchParams.get('event')!==weakened.oid);
 await page.goto(url(bash,'request'));await page.waitForSelector('.request-row');
 const task=bash.records.find(r=>r.name==='bash').task;
 assert.match(await page.locator('#detail').innerText(),new RegExp(task));
 await page.click('[data-tab="record"]');assert.match(await page.locator('#detail pre').innerText(),new RegExp(bash.oid));
 const child=data.conversations.find(c=>c.identity.owner);
 await page.locator(`[data-conversation="${child.id}"]`).click();
 assert.equal(new URL(page.url()).searchParams.get('conversation'),child.id);
 await page.fill('#search','does-not-exist-7788');assert.match(await page.locator('#events').innerText(),/No matching events/);
 await page.fill('#search','');await page.selectOption('#filter','changes');
 assert.ok(await page.locator('#events .event').count()>0);
 await page.selectOption('#example','cleanup');await page.waitForFunction(()=>document.querySelector('#facts').textContent.includes('4 conversations'));
 assert.equal(await page.locator('#conversations button').count(),4);
 // Agent-controlled text must render as text, including in changed files.
 await page.route('**/repair.json',route=>{
  const injected=structuredClone(data);
  injected.conversations.find(c=>c.id===root.id).events[0].messages=[{role:'assistant',blocks:[{text:'<img src=x onerror="window.injected=1">'}]}];
  route.fulfill({json:injected});
 });
 await page.goto(url(root.events[0]));await page.waitForSelector('#browser:not([hidden])');
 assert.match(await page.locator('#detail').innerText(),/<img src=x/);
 assert.equal(await page.evaluate(()=>window.injected),undefined);
 await page.unroute('**/repair.json');
 await page.setViewportSize({width:390,height:844});
 await page.goto(url(weakened,'files',filename));await page.waitForSelector('.file-content pre');
 const overflow=await page.evaluate(()=>document.documentElement.scrollWidth>innerWidth);
 assert.equal(overflow,false,'mobile page has horizontal overflow');
 await page.screenshot({path:process.env.SCREENSHOT_DIR?path.join(process.env.SCREENSHOT_DIR,'browser-mobile.png'):'/tmp/trajectory-browser-mobile.png',fullPage:true});
 assert.deepEqual(errors,[]);
 await browser.close();
 console.log('Passed: snapshots, diffs, deep links, requests, raw evidence, child navigation, filtering, example switching, untrusted text, mobile layout.');
})().catch(e=>{console.error(e);process.exit(1)});
